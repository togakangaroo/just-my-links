# Claude Code Prompts for just-my-links

This file contains prompts for Claude Code sessions. It serves as a persistent workspace for your interactions with Claude Code.

## Key Bindings (claude-code-prompt-mode)

| Key | Action |
|-----|--------|
| `C-c C-s` | Send section at point |
| `C-c C-r` | Send selected region |
| `C-c C-o` | Open Claude Code session |
| `C-c C-t` | Open transient menu |
| `@` | Complete file path |

## Example Prompts

Fix the bug in @src/utils.js where the parser fails on empty strings

---

Add unit tests for @src/api/auth.js

---

Refactor @src/components/Header.jsx to use React hooks

# Open source

I think I'm ready to open source with. Lets create the following tasks

- install pre-commit
- note the need to install pre-commit early in the README
- use pre-commit to set up ruff as a linter and formatter
- swap out mentions of black in our readme
- use pyright in pre-commit for a type checker
- add a pre-commit to convert readme notebook into markdown
    hooks:
      - id: export-readme-md
        name: Export README.ipynb to Markdown
        entry: uv run scripts/generate_markdown_readme.py
        files: ^README\.(ipynb|md)$
- tag reademe,ipynb cells that shouldn't appear in the export with omit_from_markdown_export

- Go through readme notebook and check it for clarity, make bits that should be executable executable
- Copy edit the readme notebook to remove redundancy. Make it less verbose, nobody wants to read a bunch of prose beyond the first paragraph explaining things.

Lets make beads for all this then implement it

# Tags

@README.md contains context.

Talk through some design with me - right now the save dialog has a "title" field and the save button. I like the simplicity of this, it prefills and often the title is relevant, but I also want to leave myself little breadcrumbs in the form of tags. What do you think of just allowing me to put hash tags in the title bar?

I suppose then I would want to be able to search for and surface my tags - maybe even list them eventually (probably via the cli to start). Currently we use an embeddings search to find documents, but maybe I should store titles and all hash tags (extracted) separately and weight matches with those more heavily. Lets say that the things that should go into the final calculation would be:

- embeddings match (1 part)
- nontrivial subphrase match with title (2 parts)
- hash tags match - exact-match of lower case #hashtags in your search (2 parts for each matched tag)

Maybe we could even display those lists separately until I build up an instinct for how it should go?

What do you think of that design?

# Next

I'm the only one using it so absolutely we can change stuff, I haven't introduced versioning yet fo that purpose.

I like the idea of going with multiple lists - no need to guess relative importance or do sorting yet.

If we do that though, lets gather some metrics. Present all three lists in a random order. They can still be labeled which is which, I'm not trying to do a double blind study just avoid an obvious source of bias. When I click a link from the search, tally it in local storage. Then log the amount of clicks on each list into the extension console

Does that sound good? Feel free to debate me on these decisions or to say that this is a tight design and we can move on to planning

# don't show it there

showing the distribution on open I think makes sense but I don't like it to be visible by default - again, a source of bias. How about we make an `<output>` element on the form that appears as a black horizontal stripe. Clicking on it reveals the distribution

# Trigger it

Oh thats interesting. Yes lets layer some complexity in there. I like how it works now with the default being a new tab taking focus and the extension closing. But I want that if I'm holding down ctrl the link opens in a tab *without* switching to it and the form stays open

# Search interface

Talk me through what you're thinking for the search interface

# Storage

That feels fine. Now lets talk about how the data storage would work on this. we're already using sqlite-vec, can we just use that same system for this? Does sqlite-vec include a sqlite db?

# Schema it

I'm going to thread the conversation a bit:

===

For migrations, I'm fine with just running a script

  AWS_PROFILE=just-my-links uv run scripts/document-db/migrations/001-add-tags.py

I suppose we should also create a 000 with the initial schema while we're at it.

These would use script dependencies so I wouldn't have to worry about dependencies and you can just use python to preform data migrations, etc.

I guess we also should also store in the db which of these migrations have been applied

Does the above make sense?

===


I'm not sure how your recommendation works for a title search. I was thinking that we have a list of stop words that are not worth indexing and we simply remove those both from the title we store and the search query. We should also store full_title for the originally entered title and remove only hash marks from both. We could then do exact substing search.

===

I'm open to the proposed structure for documents but I'm just not sure why we wouldn't just have an explicit index for tags. Seems like that would be worth it even if we put it in another table

# Pre-Plan it

Before we plan, give me a checklist of the things we talked about for me to review - no more than two sentences per item

# Post Pre Plan it

That's a start, let me tweak that for you. I think this is the order in which you should do things


- Set up Migration scripts — numbered scripts in scripts/sqlite-documents-db/migrations/, just self-contained uv python scripts using uv script dependencies and following an `\d\d\d-*.py`. naming convention
  > Lets make a scripts/sqlite-documents-db/migrations/framework.py for helper functions our migrations can import freely.
  > I can tell you up front that a helper context manager function for loading, exposing, and saving the sqlite file is a good idea. This should also create a backup of it before any modification in a git ignored file (print the backup location). This will allow us to avoid having to worry about rollbacks. It should be possible to provide a path to the sql db to use rather than loading it from s3. we can provide that as an optional environment variable
  > Another context manager that reads from migrations and compares to the currently running script to see if it should run its body as well
- Create a 000 migration script for current state. creates the current schema
  > we won't run this at all, it is for our records
- A 001 script that creates a migrations table seeded with the fact that both the 000 script and 001 script have been applied.
  > Test this against a local copy of the db
  > After success, actually run it against the real db
- Find a list of common stop words and save it as data in our index lambda code
-  A 002 script that applies the changes we need to our db
  > a document_tags table — separate table with (url, tag) url primary key and an index on tag. a tag is any word with a #prefix
  > a documents table (url, title, full_title) - full title is exactly what was saved, title is what will be searched for subphrase matches
  > a data migration to parse currently stored documents and enter them into both of these tables. Tags are lower(any #words). Titles should be lower(remove '#' character and stop words)
  > Test this against a local copy of the db
- Create an integration tests folder
  > store a copy of the migrated db there.
  > Create a README documenting what this folder is
- Rewrite the Index API to store title and hash tags as well as embeddings
  > Write an integration test against the local db copy first, then do the implementation to get it working
- Rewrite the search API to respond with three named sections (vector, title, tags), top applies per section.
  > Write an integration test against the local db copy first, then do the implementation to get it working. don't overspecify things, just make sure all three searches actually run
  > emeddings matches should work as they already do, title matches should partial search title for lower(remove '#' character and stop words), tag search should sort by number of matching hash tags between the query and matching urls. If there are no hash tags in the query, tag search should return nothing
- Do a deploy (commit first since deploys require a commit)
- Run the 002 migration against the real sql db
- Update the chrome extension. no tests needed
  > Client shuffles section order — sections rendered in random order each search to reduce position bias, but still labeled.
  > Click attribution — localStorage tracks clicks and impressions per section type. Stats logged to console on popup open, not at click time.
  > Stats reveal stripe — an <output> element styled as a black horizontal bar; click to reveal the distribution, re-hides on each new search to avoid passive bias.
  > Ctrl+click — opens result in a background tab without switching, keeps popup open. Click attribution fires regardless of modifier.
- Stop and let me test things before final commit and push

Does this make sense? Anything I'm missing before we expand on this?

# And go

Alright, we just created a series of beads. You said:


All wired up. The dependency chain looks right — three things are ready to start in parallel right now: framework (wf1), stop words (xn8), and 000 migration (55a). Everything else unlocks sequentially from there.

lets gooo!

# Android App

## Talk about it

Lets talk about the android app. My desire here is to provide two features. First, the ability to search bookmarks on my device and second the ability to save bookmarks. That latter bit is a challenge because we want to be able to grab the full text of the web page to send to the backend. Is it possible to have a chrome extension on android that works similarly to what we have on desktop? my impression is no

## Narrow it down

So yes, I was thinking the in-app browser view is probably the best option but I want to be able to share a link from the chrome browser into it which should open that link in the app browser. I want search to be in the app too just because this is where our bearer token and search url would be stored
